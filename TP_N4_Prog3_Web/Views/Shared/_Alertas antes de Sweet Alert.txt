@if (TempData["Errores"] != null)
{
    var errores = TempData["Errores"].ToString().Split("|");

    <div class="alert alert-danger">
        <ul>
            @foreach (var error in errores)
            {
                <li>@error</li>
            }
        </ul>
    </div>
}

@if (TempData["Exito"] is string exito)
{
    <div class="alert alert-success">
        <ul>
            @exito
        </ul>
    </div>
}